import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Inputs = () => {
  return (
    <View>
      <Text>Inputs</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
